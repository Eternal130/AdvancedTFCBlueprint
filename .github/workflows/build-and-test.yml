
name: Build and test

on:
  pull_request:
    branches: [ tfcp, tfc ]
  push:
    branches: [ tfcp, tfc ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
